# TEST NAME The identity pattern is accepted and computes.
# TEST SET expr

block {
  declare f : Type -> Type;
  f x = x;
  f Type
}

# TEST SET type

Type

# TEST DEFINITION

REQUIRE(expr == type);
