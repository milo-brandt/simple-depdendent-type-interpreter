# TEST BEGIN
# TEST NAME Verify does not define holes in type equations.
# TEST FULL_SET compile_result

block {
  let x : U64 = _;
  axiom Fam : U64 -> Type;
  axiom fam : (x : U64) -> Fam x;

  verify fam x : Fam 5;
  x
}

# TEST DEFINITION

REQUIRE(compile_result.holds_success());
REQUIRE(!compile_result.get_value().is_okay());
