# TEST BEGIN
# TEST NAME Verify rejects false type equations in compound requirements with incorrect RHS type.
# TEST FULL_SET compile_result

block {
  verify U64 : Type = 1;
  0
}

# TEST DEFINITION

REQUIRE(compile_result.holds_success());
REQUIRE(!compile_result.get_value().is_okay());
