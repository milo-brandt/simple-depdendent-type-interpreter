# TEST BEGIN
# TEST NAME Patterns with an incorrect output type are not accepted.
# TEST FULL_SET compile_result

block {
  axiom Bool : Type;
  axiom yes : Bool;
  axiom no : Bool;

  declare not : Bool -> Bool;
  not yes = no;
  not no = yes;

  declare f : Bool -> Bool -> Bool;
  f x y = Bool;

  f yes yes
}

# TEST DEFINITION

REQUIRE(compile_result.holds_success());
REQUIRE(!compile_result.get_value().is_okay());
