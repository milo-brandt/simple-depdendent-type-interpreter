# this test currently fails :/
# TEST NAME Ill-typed patterns are rejected.
# TEST FULL_SET compile_result

block {
  declare f : U64 -> Type;
  f U64 = 5;
  f
}

# TEST DEFINITION

REQUIRE(compile_result.holds_success()); //no syntax errors
REQUIRE(!compile_result.get_value().is_okay());
