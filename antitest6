block {
  axiom Nat : Type;
  axiom zero : Nat;
  axiom succ : Nat -> Nat;

  declare pred : Nat -> Nat;
  pred zero = zero;
  pred (succ n) = n;

  declare f : Nat -> Nat;
  f (pred x)
    where pred x = succ x
  = x;

  f zero
}
