block {
  axiom Nat : Type;
  axiom zero : Nat;

  axiom Wrap : Type -> Type;
  axiom wrap : (T : Type) -> T -> Wrap T;

  declare unwrap : (T : Type) -> Wrap T -> Type;
  unwrap T (wrap T x) = T;

  unwrap Nat (wrap Nat zero)
}
